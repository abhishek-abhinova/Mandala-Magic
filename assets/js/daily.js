/* ============================================================
   MANDALA MAGIC BY OM — Daily Mandala page
   (Widget renders through app.js; this page needs no extra logic.)
   ============================================================ */
window.MM = window.MM || {};
window.MM.pageInit = function () {};